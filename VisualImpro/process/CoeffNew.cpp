#include "../utilities.hpp"


#include <assert.h>
#include <math.h>
#include <stdio.h>

using namespace std;

extern "C"{

/** The "CoeffNew" name is only temporary, we will replace it
  * by a name matching with the coreelation function we will use here,
  * then we could delete this comment.
*/

float CoeffNew(vector<float> s1, vector<float> s2){
  return 0.0f;
}

}
